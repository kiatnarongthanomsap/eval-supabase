# 🚀 Quick Start Guide - Supabase Migration

## ✅ สิ่งที่ทำเสร็จแล้ว

1. ✅ ติดตั้ง Supabase client library
2. ✅ สร้าง API routes ทั้งหมด
3. ✅ อัปเดต frontend code
4. ✅ ตั้งค่า environment variables (`.env.local`)

## 📝 ขั้นตอนการเริ่มใช้งาน

### 1. ตั้งค่า Database Schema

**สำหรับโปรเจคใหม่ (แนะนำ):**
1. ไปที่ [Supabase Dashboard](https://app.supabase.com/project/uuharymonhyvyqxvlkqk)
2. เปิด **SQL Editor**
3. คัดลอกเนื้อหาจากไฟล์ `supabase/schema.sql`
4. วางและรัน SQL script (จะสร้างตารางทั้งหมดใหม่)

**สำหรับโปรเจคที่มีตารางอยู่แล้ว:**
1. ไปที่ **SQL Editor**
2. คัดลอกเนื้อหาจากไฟล์ `supabase/fix-schema.sql`
3. วางและรัน SQL script (script นี้จะเพิ่ม columns ที่ขาดหายไป)

### 2. (Optional) Import ข้อมูลจาก MySQL

หากมีข้อมูลจาก MySQL database เดิม:

**วิธีที่ 1: ใช้ Supabase Dashboard**
1. Export ข้อมูลจาก MySQL เป็น CSV
2. ไปที่ Supabase Dashboard > **Table Editor**
3. เลือกตารางที่ต้องการ
4. คลิก **Import** และเลือกไฟล์ CSV

**วิธีที่ 2: ใช้ SQL Script**
1. ดูไฟล์ `supabase/migrate-data.sql` สำหรับตัวอย่าง
2. ปรับแต่ง SQL statements ตามข้อมูลของคุณ
3. รันใน Supabase SQL Editor

### 3. เริ่ม Development Server

```bash
npm run dev
```

### 4. ทดสอบระบบ

1. เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
2. ทดสอบการ Login
3. ทดสอบการบันทึกคะแนน
4. ตรวจสอบข้อมูลใน Supabase Dashboard

## 🔧 Service Role Key (Optional)

Service Role Key ใช้สำหรับ server-side operations ที่ต้องการ bypass Row Level Security

**วิธีหา Service Role Key:**
1. ไปที่ Supabase Dashboard > **Settings** > **API**
2. คัดลอก **service_role** key (⚠️ ระวัง: อย่า expose key นี้ให้ client)
3. เพิ่มใน `.env.local`:
   ```env
   SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
   ```

**หมายเหตุ:** สำหรับการใช้งานพื้นฐานไม่จำเป็นต้องใช้ Service Role Key เพราะเราใช้ anon key ได้

## 🐛 Troubleshooting

### ปัญหา: "Missing Supabase URL or Anon Key"

**แก้ไข:** 
- ตรวจสอบว่าไฟล์ `.env.local` อยู่ใน root directory
- Restart development server (`npm run dev`)

### ปัญหา: "Row Level Security policy violation"

**แก้ไข:**
- ตรวจสอบว่า schema SQL ถูกรันเรียบร้อยแล้ว
- ตรวจสอบ RLS policies ใน Supabase Dashboard > **Authentication** > **Policies**
- สำหรับ development อาจใช้ permissive policies

### ปัญหา: API routes ไม่ทำงาน

**แก้ไข:**
1. ตรวจสอบ console logs
2. ตรวจสอบ Supabase Dashboard > **Logs** > **API Logs**
3. ตรวจสอบว่า environment variables ถูก load

## 📚 เอกสารเพิ่มเติม

- `SUPABASE_SETUP.md` - คู่มือการตั้งค่าแบบละเอียด
- `supabase/schema.sql` - Database schema
- `supabase/migrate-data.sql` - ตัวอย่างการ import ข้อมูล

## ✅ Checklist

- [ ] รัน database schema ใน Supabase SQL Editor
- [ ] (Optional) Import ข้อมูลจาก MySQL
- [ ] ทดสอบ development server (`npm run dev`)
- [ ] ทดสอบการ Login
- [ ] ทดสอบการบันทึกคะแนน
- [ ] ตรวจสอบข้อมูลใน Supabase Dashboard
- [ ] (Optional) ตั้งค่า Service Role Key

## 🎉 พร้อมใช้งาน!

ระบบพร้อมใช้งาน Supabase แล้ว! หากพบปัญหาหรือต้องการความช่วยเหลือเพิ่มเติม ดูที่ `SUPABASE_SETUP.md`

