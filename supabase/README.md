# Supabase Database Setup

คำแนะนำการตั้งค่าฐานข้อมูล Supabase สำหรับระบบประเมินผลงาน HR

## 📋 ไฟล์ที่มี

1. **schema.sql** - โครงสร้างฐานข้อมูล (Tables, Indexes, Triggers, RLS)
2. **import-data.sql** - ข้อมูลเริ่มต้น (Users, Criteria, Exclusions, Config)

## 🚀 ขั้นตอนการติดตั้ง

### 1. สร้างโปรเจค Supabase

1. ไปที่ [https://supabase.com](https://supabase.com)
2. สร้างโปรเจคใหม่
3. บันทึก **Project URL** และ **Anon Key**

### 2. รัน Schema

1. ไปที่ Supabase Dashboard → **SQL Editor**
2. คลิก **New Query**
3. คัดลอกเนื้อหาจาก `schema.sql`
4. วาง (Paste) และคลิก **Run**

✅ ระบบจะสร้างตารางทั้งหมด 7 ตาราง:
- `users` - ข้อมูลผู้ใช้
- `criteria` - หลักเกณฑ์การประเมิน
- `evaluations` - คะแนนประเมิน
- `comments` - ความคิดเห็น
- `exclusions` - ข้อยกเว้นการประเมิน
- `system_config` - การตั้งค่าระบบ
- `logs` - บันทึกการใช้งาน

### 3. Import ข้อมูล

1. ใน **SQL Editor** สร้าง New Query อีกครั้ง
2. คัดลอกเนื้อหาจาก `import-data.sql`
3. วาง (Paste) และคลิก **Run**

✅ ระบบจะ import ข้อมูล:
- **51 Users** - พนักงานและกรรมการ
- **27 Criteria** - หลักเกณฑ์การประเมิน
- **13 Exclusions** - ข้อยกเว้นการประเมิน
- **10 System Configs** - การตั้งค่าระบบ

### 4. ตรวจสอบข้อมูล

1. ไปที่ **Table Editor**
2. เลือกตารางที่ต้องการดู
3. ตรวจสอบว่าข้อมูล import สำเร็จ

## 🔐 Row Level Security (RLS)

Schema ได้เปิดใช้งาน RLS และสร้าง Policy พื้นฐาน:

- **Allow public read access** - อนุญาตให้อ่านข้อมูลได้ทุกคน
- **Allow all operations** - อนุญาตให้แก้ไขข้อมูลได้ทุกคน (สำหรับ Development)

⚠️ **สำคัญ**: สำหรับ Production ควรปรับ RLS Policy ให้เข้มงวดมากขึ้น

## 🔑 Environment Variables

อัปเดตไฟล์ `.env.local` ในโปรเจค Next.js:

```bash
NEXT_PUBLIC_SUPABASE_URL=https://fcezmzydokhxlajxaxdq.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_jnmwgsWStQVoq7i3mzBhDA_Vdq_fzCx
```

## 📊 โครงสร้างตาราง

### users
- ข้อมูลผู้ใช้ทั้งหมดในระบบ
- มีความสัมพันธ์แบบ Parent-Child (parent_internal_id)
- มี Role: COMMITTEE, MANAGER, ASST, HEAD, STAFF

### criteria
- หลักเกณฑ์การประเมิน 3 ประเภท
- PERF = ผลการปฏิบัติงาน (Performance)
- CHAR = คุณลักษณะ (Characteristics)
- EXEC = ผู้บริหาร (Executive)

### evaluations
- คะแนนการประเมิน (1-4)
- มี Unique constraint: (evaluator, target, criteria)
- อัปเดต updated_at อัตโนมัติ

### comments
- ความคิดเห็นสำหรับแต่ละคน
- มี Unique constraint: (evaluator, target)

### exclusions
- กำหนดว่าใครไม่ต้องประเมินใคร
- ใช้ org_id ในการอ้างอิง

### system_config
- การตั้งค่าระบบ (start_date, end_date, SMTP, etc.)
- เก็บเป็น Key-Value pairs

### logs
- บันทึกการใช้งานระบบ
- มี timestamp, type, message

## 🔄 การ Import ข้อมูลซ้ำ

ไฟล์ `import-data.sql` ใช้ `ON CONFLICT` ทำให้:
- สามารถรันซ้ำได้โดยไม่เกิด Error
- จะอัปเดตข้อมูลที่มีอยู่แล้ว
- เหมาะสำหรับ sync ข้อมูลจาก MySQL

## 📝 หมายเหตุ

- ไฟล์ SQL นี้แปลงจาก MySQL มาเป็น PostgreSQL
- ข้อมูล Evaluations และ Comments ไม่ได้ import (ควรสร้างใหม่)
- password field ยังว่างเปล่า (ใช้ OTP Login)
- SMTP Config อยู่ใน system_config

## 🆘 การแก้ปัญหา

### Error: relation already exists
- ตาราง/index มีอยู่แล้ว
- ใช้ `DROP TABLE IF EXISTS` ก่อน (ระวัง: จะลบข้อมูล)

### Error: column does not exist
- ตรวจสอบว่ารัน `schema.sql` เรียบร้อยแล้ว
- ตรวจสอบชื่อ column ในไฟล์ SQL

### Error: permission denied
- ตรวจสอบ RLS Policies
- อาจต้องปิด RLS ชั่วคราวสำหรับ import ข้อมูล

## 📚 เอกสารเพิ่มเติม

- [Supabase Documentation](https://supabase.com/docs)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)
- [QUICK_START.md](../QUICK_START.md) - คำแนะนำการเริ่มต้นใช้งาน
- [SUPABASE_SETUP.md](../SUPABASE_SETUP.md) - คู่มือการตั้งค่าฉบับเต็ม

